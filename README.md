Run training (configuration file in the config_files folder):
$ python -m AI_code_challenge --mode Train --config config_files/example_config.json

Run Test:
$ python -m AI_code_challenge --mode Test --config config_files/example_config.json

